# Steganografi LSB + AES

This project demonstrates how to hide encrypted data into digital images using LSB steganography and AES encryption.

## Folder Structure
- `samples/input`: cover images
- `samples/secret`: secret files to hide
- `samples/stego`: generated stego images
- `outputs`: extracte
